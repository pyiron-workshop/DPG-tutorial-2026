#!/bin/bash
git clone https://github.com/pyiron/pyiron_core.git
cd pyiron_core
rm -r pyiron_core/pyiron_nodes
mkdir pyiron_core/pyiron_nodes
touch pyiron_core/pyiron_nodes/__init__.py
pip install . --no-deps --no-build-isolation
cd ..

# Now clean up everything else
rm -r pyiron_core
rm -r .github
rm _config.yml _toc.yml README.md environment.yaml CITATION.cff postBuild